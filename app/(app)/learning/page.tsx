// Fallacy/argument documentation + AI chatbot helper. Nothing built yet.
export default function LearningPage() {
  return (
    <div style={{ padding: 24, color: "var(--muted)" }}>
      Learning tab — coming soon.
    </div>
  );
}
