// Friends system. Schema exists (friendships table) — needs: username
// setup prompt, add/search friends UI, pending-request handling, and the
// challenge-a-friend flow into online_matches.
export default function OnlineFriendsPage() {
  return (
    <div style={{ padding: 24, color: "var(--muted)" }}>
      Friends — coming soon.
    </div>
  );
}
