// Profile name/avatar editing (currently lives in ProfileModal, only
// reachable from the menu screen), sign out, and eventually theme
// selection once the Store exists.
export default function SettingsPage() {
  return (
    <div style={{ padding: 24, color: "var(--muted)" }}>
      Settings — coming soon.
    </div>
  );
}
