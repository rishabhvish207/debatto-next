// Profile name/avatar editing already lives on the /profile page; this tab
// will eventually cover sign out and theme selection once the Store exists.
export default function SettingsPage() {
  return (
    <div style={{ padding: 24, color: "var(--muted)" }}>
      Settings — coming soon.
    </div>
  );
}
