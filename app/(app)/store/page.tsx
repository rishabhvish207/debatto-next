// Theme/CSS purchasing. Schema exists (shop_items, user_purchases,
// mirroring the debots/user_debots unlock pattern) — needs actual items
// seeded and a purchase UI reusing the same confirm-in-panel pattern as
// DebotStage's unlock flow.
export default function StorePage() {
  return (
    <div style={{ padding: 24, color: "var(--muted)" }}>
      Store — coming soon.
    </div>
  );
}
